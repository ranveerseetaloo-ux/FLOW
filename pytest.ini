[pytest]
# importlib mode lets the two identically-named `tests/` packages
# (data-plane + control-plane) coexist without an import collision.
addopts = --import-mode=importlib
testpaths =
    data-plane/tests
    control-plane/tests
